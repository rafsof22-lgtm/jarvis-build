# Jarvis Build Mammoth Consolidated Specification — V21 Builder Evidence Sync

**State:** `IMPLEMENTED_REPOSITORY_CANDIDATE_PENDING_CI_REVIEW`  
**Melbourne date:** 23 July 2026  
**Merged baseline:** V19 `3d5e7344706cae2909ebad2aa540078464285db2`  
**Dependency baseline:** open V20 PR #105 `334f4d39a51fbcb536d8db83d7bf5b7c739d85ed`  
**Proposed branch:** `jarvis/v21-builder-evidence-sync-20260723`  
**Target:** Original Jarvis Framework  
**Builder:** Jarvis Build  
**Overlays:** sovereign role council, request compiler, forensic audit, universal execution, Prompt Cowboy QA, SOP/OJT

## 1. Proof boundary

This specification consolidates all accessible evidence in this run. It does not claim 100% completion. The 23 July flattened export lacks native ChatGPT IDs/trees/system/tool nodes, 383 classifications require review, 1,069 Jarvis applicability links require decision, authenticated staging is incomplete, and production is not authorised.

## 2. Source denominator

- Flattened export: **2669 conversations / 97589 messages**.
- Jarvis-relevant candidate universe: **1508 conversations / 42994 user messages / 45352 assistant messages**.
- All candidate user messages received privacy-safe mapping: **42994 / 42994**.
- Assistant commitments extracted: **33322**.
- Blocker/open-loop references extracted: **14194**.
- Mapping root SHA-256: `7d0cd7a21558fc39dadca23e4acf88b9181c8853090386559270e2dcccf4b698`.
- V34 prior source rescan: 31 files / 121,911,922 extracted characters.
- V35 baseline: 38 module manifests.

Raw private message text remains local. Public repository artifacts contain hashes, pointers, categories, counts, canonical requirements, and evidence boundaries only.

## 3. Architecture separation

`ORIGINAL_JARVIS_TARGET` is the operating system being reconstructed. `JARVIS_BUILD_BUILDER` performs intake, compilation, build, verification, repository and deployment work. `CONTROL_OVERLAY` layers strengthen execution but cannot replace or rename the target. Domain modules remain isolated. Shared services require versioned contracts.

## 4. Canonical architecture

`Experience -> Gateway -> Identity/Consent -> Sovereign Control Plane -> Agent Council -> Skill/Tool Fabric -> Model Router -> Workflow/Event Runtime -> Domain Services -> Data/State -> Memory/Knowledge -> Evidence/Audit -> Security -> Observability/Cost -> Verification/Simulation -> Infrastructure/Deployment -> Continuity/Rollback -> Source Reconstruction/Learning`

## 5. Canonical registries

The V21 package creates or updates:

- REQUIREMENTS_REGISTER, REQUEST_REQUIREMENT_LINKS and privacy-safe request denominator;
- MODULE, FEATURE, FUNCTION and SKILL registries;
- TOOL, API, MODEL and WORKFLOW registries;
- SOP and OJT qualification registries;
- APPROVAL, GAP, EVIDENCE and DEPLOYMENT ledgers;
- rollback runbook, resume prompt, source/extraction audit, completion-state JSON and PR handoff.

## 6. Requirements, features and functions

All 42994 candidate user messages are mapped to one or more of 38 canonical requirement families. Every canonical requirement links to a feature, function, primary module, source counts, test/evidence path, runtime state, rollback and owner-acceptance state. Detailed mappings remain in the local evidence pack; compact canonical registries are GitHub-ready.

## 7. Module system

The module registry contains 51 canonical/baseline modules, including 38 V35 runtime/control manifests and isolated domain modules for Master KB, XRP/HBAR, trading research, AI CFO, Health, Property, Automotive/Jenok, Video, Passive Income, Survival, Spooky2, Dream Home, Grow, and Personal Admin/Legal.

No module self-certifies. Every module must reconcile:

`Requirement -> Contract -> Artifact -> Test/Waiver -> Evidence -> Runtime -> Rollback -> Owner acceptance`.

## 8. Skills

The Skill registry preserves installed specialist ownership. `jarvis-sovereign-builder` remains the integration spine; `master-chatgpt-kb` owns source truth; `jarvis-reconstruction` owns reconstruction workflow; domain Skills retain stricter domain gates. Skills are routed, not copied into the control plane.

## 9. Tools, apps and APIs

Default route: verified artifact/cache -> deterministic local Python/SQLite -> connected native APIs -> local/open model -> free allowance -> lowest-cost suitable paid route -> specialist premium route only when justified. V21 used deterministic local Python/SQLite and GitHub-native connector operations; no paid model/API call is required for the extraction or package.

## 10. Request compiler

For every request: preserve hash/pointer, classify, derive explicit/implied requirements, map to module/feature/function/Skill/tool/API/workflow/test/evidence/rollback, preserve conflicts, and assign terminal state. Owner-gated actions are captured but never auto-executed.

## 11. Role council

Human Owner, Constitutional Guardian, Lead Data Architect, Forensic Linguistic Auditor, Taxonomy Specialist, Request Compiler, Architecture Planner, Builder, Security/Privacy Authority, Cost Governor, Domain Chiefs, Independent Verifier, Release Gatekeeper, Continuity Archivist, and Incident/Rollback Controller remain logically separated.

## 12. Security and privacy

- no raw secret values;
- no raw private chat text in public GitHub;
- least privilege, service isolation, provenance, supply-chain review, prompt-injection/memory-poisoning controls;
- module-local data, credentials, queues, routes, backups, restore and rollback;
- fail-closed review for legal, finance, health, money movement, trading, publication and destructive actions.

## 13. Deployment lifecycle

`LOCAL -> TEST -> PR/CI -> AUTHENTICATED_STAGING -> CANARY -> CONTROLLED_PRODUCTION -> FINAL_ACCEPTANCE`.

V21 may create branch and PR under this request. Merge remains owner-gated. Staging requires approved IAM, secret references, endpoints, databases, budgets, observability, backups and rollback owners. Production requires a separate approval.

## 14. PR #105 decision

V21 branches from the V20 head so V20 evidence is preserved and not duplicated. PR #105 remains open during V21 review. If V21 is approved and merged, #105 should be closed as superseded with audit history preserved. It must not be merged independently after V21 has incorporated its commits.

## 15. Testing and evidence

Required V21 checks:

- search tool unit tests and local database smoke;
- exact candidate-message mapping count reconciliation;
- registry foreign-key/logical-link validation;
- target/builder/overlay invariant;
- unsupported completion-claim scan;
- secret-like assignment scan;
- JSON/CSV/Markdown validation;
- existing repository historical verifiers and CI;
- independent PR review before merge.

## 16. Remaining gaps

See `registers/GAP_REGISTER.csv`. Critical gates remain native export metadata, 383 classification reviews, 1,069 applicability decisions, commitment-to-artifact reconciliation, current runtime proof, authenticated staging, canary, production and owner acceptance.

## 17. Next actions

1. Create `jarvis/v21-builder-evidence-sync-20260723` from V20 head.
2. Commit this privacy-safe V21 package and open the requested PR.
3. Verify CI/security/check existence and record the PR link.
4. Do not merge until explicit owner approval.
5. After merge approval, resolve staging credentials/configuration one service at a time.
